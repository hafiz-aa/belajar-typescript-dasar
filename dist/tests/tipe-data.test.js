"use strict";
describe("Data Type", () => {
    it("Should must declare", function () {
        const name = "Muhammad Hafiz";
        const balance = 100000;
        const isVip = true;
        console.log(name);
        console.log(balance);
        console.log(isVip);
    });
});
