"use strict";
describe("Data Type", () => {
    it("Should must declare", function () {
        let name = "Muhammad Hafiz";
        let balance = 100000;
        let isVip = true;
        console.log(name);
        console.log(balance);
        console.log(isVip);
    });
});
